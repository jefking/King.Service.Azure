# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore King.Service.Storage dependencies
      run: dotnet restore King.Service.Storage/King.Service.Storage.csproj
    - name: Restore Demo dependencies
      run: dotnet restore King.Service.Storage.Demo/King.Service.Storage.Demo.csproj
    - name: Build King.Service.Storage
      run: dotnet build --no-restore King.Service.Storage/King.Service.Storage.csproj
    - name: Build Demo
      run: dotnet build --no-restore King.Service.Storage.Demo/King.Service.Storage.Demo.csproj
    - name: Test King.Service.Storage
      run: dotnet test --no-build --verbosity normal King.Service.Storage.Tests/King.Service.Storage.Tests.csproj